eureka:
  client:
    service-url:
       defaultZone: http://localhost:8761/eureka/
spring:
  cloud:
    config:
      discovery:
        service-id: config-server
        enabled: true
      profile: default
      label: master
  application:
    name: consume2