eureka:
  client:
    service-url:
       defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: resource2
  cloud:
    config:
      discovery:
        service-id: config-server
        enabled: true
      profile: default
      label: master




